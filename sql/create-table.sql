CREATE TABLE `Rocket` (
    `id` int(6) unsigned NOT NULL AUTO_INCREMENT,
    `rocket_name` varchar(30) NOT NULL,
    `payload_weight` varchar(30) NOT NULL,
    `rocket_type` varchar(30) NOT NULL,
    PRIMARY KEY (`id`)
 ) ENGINE=InnoDB AUTO_INCREMENT=1;